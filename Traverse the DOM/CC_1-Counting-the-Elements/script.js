// TODO: write the countElements() function here
// Complete the following program to write the countElements() function, 
// that takes a CSS selector as a parameter and returns the number of corresponding elements:

const countElements = myElement => document.querySelectorAll(myElement).length;
